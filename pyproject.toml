[tool.isort]
py_version=311
profile = "black"
src_paths = ["src", "tests"]
overwrite_in_place = true

[tool.black]
target-version = ["py311"]

[tool.ruff]
target-version = "py311"
select = ["E", # default
	  "F", # default
	  "I", # isort
	  "UP", # pyupgrade
	 ]

[tool.pyright]
pythonVersion = "3.11"
include = ["src", "tests"]
